// Grab the day of the week from local computer
// const days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'];
// let current = 0;

/* function weekday(){
    document.getElementById('weekday').innerHTML = days[current++ % days.length];
} */

// This wasn't needed in the end - the JS ran through the index.html file. Before I could figure out how to get the current time on the button click, I had the button rotating through Monday - Sunday at random.
